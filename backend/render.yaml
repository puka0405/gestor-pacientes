services:
  - type: web
    name: pacientes-api
    env: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn run:app

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pacientes-db
          property: connectionString

      - key: SECRET_KEY
        generateValue: true